# this is a comment
echo This is an example message

echo This directory is
pwd

echo And has
ls

echo Lets create a file
touch file.txt
ls

echo and now lets delete it
rm file.txt
ls

# lets create a variable
var foo=bar
# and then print it
echo The value of the variable foo is [ $(foo) ]